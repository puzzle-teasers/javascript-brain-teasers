// NaN
// declares a constant variable without assigning any value to it. So, the variable holds a default value
// of undefined. When trying to add undefined to the value of price, the
// operation yields NaN (Not-a-Number).
let price = 10;
let tax;

const sum = price + tax;

console.log(sum);